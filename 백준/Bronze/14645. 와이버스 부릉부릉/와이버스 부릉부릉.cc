#include <iostream>
using namespace std;


int main() {
	int n, k = 0;
	cin >> n >> k;
	for (int i = 0; i < n; i++) {
		int a, b;
		b = k;
		cin >> a >> b;
	}
	cout << "비와이" << endl;
	return 0;
}